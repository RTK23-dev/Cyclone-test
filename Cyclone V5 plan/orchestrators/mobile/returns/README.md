# Mobile agent returns

Write `RETURN-00N-<slug>.md` here using [`../../TEMPLATES/AGENT-RETURN.md`](../../TEMPLATES/AGENT-RETURN.md).

Must include: PR URL on `premiumcentraal-boop/Cyclone`, head SHA, paths touched, tests, leftover. No secret values in the return either.
