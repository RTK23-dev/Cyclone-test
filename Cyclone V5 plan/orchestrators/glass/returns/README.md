# Glass agent returns

Write `RETURN-00N-<slug>.md` here using [`../../TEMPLATES/AGENT-RETURN.md`](../../TEMPLATES/AGENT-RETURN.md).

Must include: PR URL on `premiumcentraal-boop/Cyclone`, head SHA, paths touched, tests (`npm test` in `apps/pc-companion` where you added coverage), leftover. No secret values.
